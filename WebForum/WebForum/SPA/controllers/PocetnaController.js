webForum.controller('PocetnaController', function ($scope, PodforumiFactory, TemeFactory, KomentariFactory, $routeParams) {

    function init() {
        console.log('Pocetna controller inicijalizovan');
    }

    init();

});